import 'package:flutter/material.dart';

class EmployeeColleguesScreen extends StatelessWidget {
  const EmployeeColleguesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Collegue Screen', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
